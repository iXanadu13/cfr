/*
 * Decompiled with CFR.
 */
package org.benf.cfr.tests;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.function.Function;

public class LambdaTest8 {
    static <T, R> List<R> map(Function<T, R> function, List<T> source) {
        ArrayList<R> destiny = new ArrayList<R>();
        for (T item : source) {
            R value = function.apply(item);
            destiny.add(value);
        }
        return destiny;
    }

    public void test() {
        List<String> digits = Arrays.asList("1", "2", "3", "4", "5");
        List<Integer> numbers = LambdaTest8.map(x -> LambdaTest8.mkInt(x), digits);
    }

    private static Integer mkInt(String intStr) {
        return new Integer(intStr);
    }
}

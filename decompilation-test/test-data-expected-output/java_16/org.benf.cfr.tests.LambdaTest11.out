/*
 * Decompiled with CFR.
 */
package org.benf.cfr.tests;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Set;
import java.util.function.Function;
import java.util.function.Supplier;
import org.benf.cfr.tests.support.SetFactory;

public class LambdaTest11 {
    static <T, R> List<R> map(Function<T, R> function, List<T> source) {
        List destiny = LambdaTest11.create(ArrayList::new);
        for (T item : source) {
            R value = function.apply(item);
            destiny.add(value);
        }
        return destiny;
    }

    public static <V> V create(Supplier<V> factory) {
        return factory.get();
    }

    public void test() {
        List<String> digits = Arrays.asList("1", "2", "3", "4", "5");
        Set s = SetFactory.newSet();
        List<Boolean> numbers = LambdaTest11.map(s::contains, digits);
    }
}
